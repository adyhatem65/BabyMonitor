import {StyleSheet} from 'react-native';
import {fontFamily, FONTS} from '../../utils';

export const styles = StyleSheet.create({
  textStyle: {
    fontSize: FONTS.FONT_16,
    fontFamily: fontFamily.medium,
  },
});
